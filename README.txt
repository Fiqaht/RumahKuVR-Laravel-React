Merge this patch into the root of the current RumahKuVR Laravel project and choose Replace files in destination.

Then keep Laravel running and restart Vite:
  Ctrl + C
  npm run dev

Hard refresh browser:
  Ctrl + Shift + R

This patch is intended for the earlier Future Mono Elder-Friendly version.
